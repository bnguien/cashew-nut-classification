# MQTT broker tối thiểu cho demo PBL5 (LAN + ESP + Django)
# Chạy: "C:\Program Files\Mosquitto\mosquitto.exe" -c "<đường dẫn đầy đủ tới file này>" -v

listener 1883 0.0.0.0
allow_anonymous true

# Tắt persistence cho demo (không cần file DB)
persistence false

log_type all
