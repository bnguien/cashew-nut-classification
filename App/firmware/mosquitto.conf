# =============================================================================
# PBL5 — Mosquitto 2.x: LAN + ESP + Django (dán vào CUỐI mosquitto.conf)
# Chạy PowerShell (Admin): xem firmware/README-MOSQUITTO.txt
# =============================================================================
listener 1883 0.0.0.0
allow_anonymous true
persistence false
log_type all
